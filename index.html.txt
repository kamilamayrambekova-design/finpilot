<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>FinPilot MVP</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-10">
  <div class="max-w-xl mx-auto bg-white p-6 rounded shadow">
    <h1 class="text-3xl font-bold mb-4">FinPilot MVP</h1>

    <h2 class="text-xl mb-2">Загрузить CSV</h2>
    <input type="file" id="csv" class="mb-4">
    <button onclick="uploadCSV()" class="bg-blue-600 text-white px-4 py-2 rounded">Загрузить</button>

    <pre id="out" class="mt-4"></pre>

    <h2 class="text-xl mt-6 mb-2">AI рекомендация</h2>
    <button onclick="ai()" class="bg-green-600 text-white px-4 py-2 rounded">Получить</button>
    <pre id="ai" class="mt-4"></pre>
  </div>

<script>
async function uploadCSV() {
  const file = document.getElementById("csv").files[0];
  const fd = new FormData();
  fd.append("file", file);
  const r = await fetch("/upload", { method: "POST", body: fd });
  document.getElementById("out").innerText = JSON.stringify(await r.json(), null, 2);
}

async function ai() {
  const r = await fetch("/ai");
  document.getElementById("ai").innerText = JSON.stringify(await r.json(), null, 2);
}
</script>
</body>
</html>
